import React from 'react';
import ReactDOM from 'react-dom';
import ResumeViewer from '@resume-creator/renderer';

const MOUNT_ELEMENT = document.getElementById('root');

ReactDOM.render(<ResumeViewer />, MOUNT_ELEMENT);
